# It is DAG(Directed Acyclic Graph) is a linear ordering of vertices such that for every directed edge uv, vertex u comes before v in the ordering.
# Topological sorting not possible if the graph is not DAG.
''